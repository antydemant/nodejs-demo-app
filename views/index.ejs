<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://unpkg.com/masonry-layout@4/dist/masonry.pkgd.min.js"></script>
    <script src="script.js"></script>
    <style>

        form {
            padding-bottom: 50px;
            margin: auto;
            width: 50%;
        }

        form input[type=text] {
            padding: 10px;
            font-size: 17px;
            border: 1px solid grey;
            float: left;
            width: 70%;
            margin: auto;
            background: #f1f1f1;
        }

        form button {
            width: 20%;
            padding: 10px;
            background: #2196F3;
            color: white;
            font-size: 17px;
            border: 1px solid grey;
            border-left: none;
            cursor: pointer;
            float: left;
        }

        .grid {
            display: none;
            margin: 0 auto;
        }

        img {
            position: relative;
        }

        .overlay {
            position: absolute;
            top: 0;
            bottom: 0;
            left: 0;
            right: 0;
            width: 100%;
            height: 100%;
            opacity: 0;
            transition: .5s ease;
            background-color: #f79071;
        }

        .overlay:hover {
           opacity: 0.9; 
        }

        .text {
            color: white;
            font-size: 20px;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-align: center;
        }
    </style>
</head>
<body>
    <form action="/" method="GET">
        <input type="text" name="hashtag" placeholder="#одуванчиктріп" />
        <button type="submit">Search</button>
    </form>
    <div class="grid">
        <% items.forEach(function(item) { %>
            <div class="grid-item">
                <img width="200" src="<%=item.node.display_url %>" />
                <div class="overlay">
                    <div class="text"><%=item.node.edge_liked_by.count %></div>
                </div>
            </div>
        <% }); %>
    </div>
</body>
</html>
